apiVersion: v1
kind: Service
metadata:
  name: angular-catastro
  labels:
    app: angular-catastro
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: angular-catastro
